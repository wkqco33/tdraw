# 보안 정책 (Security Policy)

`tdraw` 프로젝트의 보안 취약점 보고 및 처리 정책입니다.

---

## 1. 지원 버전 (Supported Versions)

보안 업데이트는 최신 메이저/마이너 버전 릴리스에 대해서만 제공됩니다.

| 버전 | 지원 여부 |
| :--- | :--- |
| `0.3.x` | ✓ 지원 중 (최신 릴리스) |
| `< 0.3.0` | ❌ 지원 종료 (최신 버전으로 업그레이드 필요) |

---

## 2. 취약점 제보 방법 (Reporting a Vulnerability)

`tdraw`에서 보안 취약점을 발견하신 경우, **공개 이슈(Issue)를 생성하지 마시고** 다음 방법으로 제보해 주시기 바랍니다:

1. **GitHub Security Advisory**: 저장소의 `Security` 탭 -> `Report a vulnerability` 비공개 보고 기능 이용
2. **이메일 제보**: 메인테이너 이메일 (GitHub 프로필 참조)로 상세 정보 발송

### 제보 시 포함할 내용

- 취약점의 성격 및 영향도 설명 (예: 메모리 오버플로우, 비정상 패닉, DoS 등)
- 재현에 필요한 최소한의 샘플 파일(이미지/비디오) 및 실행 명령어
- 취약점이 확인된 `tdraw` 버전 및 운영체제 정보
- 가능한 경우 완화 방안 또는 패치 제안

---

## 3. 취약점 처리 절차

- 제보 접수 후 48시간 이내에 확인 메일 또는 코멘트를 회신합니다.
- 취약점 유효성이 확인되면 패치를 개발하고 비공개 검증을 진행합니다.
- 패치가 준비되면 신규 패치 버전(Patch Release)을 배포하고 보안 권고를 공개합니다.
