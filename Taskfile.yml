version: '3'

vars:
  BIN: tdraw
  DISTDIR: dist
  GO: go
  GOFLAGS: -trimpath
  # 빌드 버전: git describe 결과, 실패 시 dev
  VERSION:
    sh: git describe --tags --always --dirty 2>/dev/null || echo dev
  LDFLAGS: -s -w -X main.version={{.VERSION}}
  # 설치 경로 (Unix). Windows에서는 install/uninstall 태스크가 비활성화됨
  BINDIR:
    sh: echo "${HOME}/.local/bin"
  PLATFORMS: linux/amd64 linux/arm64 darwin/amd64 darwin/arm64 windows/amd64

tasks:
  default:
    desc: 기본 태스크 (build)
    cmds:
      - task: build

  build:
    desc: 현재 플랫폼용 바이너리 빌드 (비디오 재생 미포함)
    cmds:
      - "{{.GO}} build {{.GOFLAGS}} -ldflags \"{{.LDFLAGS}}\" -o {{.BIN}} ./cmd/tdraw"

  setup:video:
    desc: 비디오 빌드/테스트용 go.work 구성 (tcamviewer 체크아웃 필요, 기본 ../tcamviewer)
    cmds:
      - test -f go.work || go work init
      - go work use . "${TCAMVIEWER_DIR:-../tcamviewer}"

  build:video:
    desc: 비디오 재생(tcamviewer) 포함 빌드 — tcamviewer 코어가 빌드되어 있어야 한다
    deps: [setup:video]
    cmds:
      - "{{.GO}} build -tags tcamviewer {{.GOFLAGS}} -ldflags \"{{.LDFLAGS}}\" -o {{.BIN}} ./cmd/tdraw"

  test:
    desc: 테스트 실행
    cmds:
      - "{{.GO}} test ./... -v"

  test:video:
    desc: 비디오 재생(tcamviewer 연동) 포함 테스트 실행
    deps: [setup:video]
    cmds:
      - "{{.GO}} test -tags tcamviewer ./... -v"

  fmt:
    desc: 코드 포맷
    cmds:
      - "{{.GO}} fmt ./..."

  vet:
    desc: 정적 분석
    cmds:
      - "{{.GO}} vet ./..."

  release-plain:
    desc: PPM용 darwin/windows 아카이브와 체크섬을 dist/ 에 생성 (순수 Go, 비디오 미포함)
    deps: [clean-dist]
    cmds:
      - mkdir -p {{.DISTDIR}}
      - |
        for platform in darwin/amd64 darwin/arm64 windows/amd64; do
          os=${platform%/*}
          arch=${platform#*/}
          binary_name="{{.BIN}}"
          archive_ext=".tar.gz"
          if [ "$os" = "windows" ]; then
            binary_name="{{.BIN}}.exe"
            archive_ext=".zip"
          fi

          binary="{{.DISTDIR}}/$binary_name"
          archive="{{.DISTDIR}}/{{.BIN}}_${os}_${arch}${archive_ext}"
          echo "빌드: $archive"
          GOOS=$os GOARCH=$arch {{.GO}} build {{.GOFLAGS}} -ldflags "{{.LDFLAGS}}" -o "$binary" ./cmd/tdraw

          if [ "$os" = "windows" ]; then
            zip -q -j "$archive" "$binary"
            unzip -tq "$archive"
          else
            tar -czf "$archive" -C "{{.DISTDIR}}" "$binary_name"
            tar -tzf "$archive" >/dev/null
          fi

          (cd "{{.DISTDIR}}" && sha256sum "$(basename "$archive")" > "$(basename "$archive").sha256")
          rm -f "$binary"
        done
      - 'echo "완료: {{.DISTDIR}}/"'
      - ls -lh {{.DISTDIR}}/

  release:
    desc: PPM용 플랫폼별 아카이브와 SHA-256 체크섬을 dist/ 에 생성 (linux는 비디오 미포함)
    deps: [clean-dist]
    cmds:
      - mkdir -p {{.DISTDIR}}
      - |
        for platform in {{.PLATFORMS}}; do
          os=${platform%/*}
          arch=${platform#*/}
          binary_name="{{.BIN}}"
          archive_ext=".tar.gz"
          if [ "$os" = "windows" ]; then
            binary_name="{{.BIN}}.exe"
            archive_ext=".zip"
          fi

          binary="{{.DISTDIR}}/$binary_name"
          archive="{{.DISTDIR}}/{{.BIN}}_${os}_${arch}${archive_ext}"
          echo "빌드: $archive"
          GOOS=$os GOARCH=$arch {{.GO}} build {{.GOFLAGS}} -ldflags "{{.LDFLAGS}}" -o "$binary" ./cmd/tdraw

          if [ "$os" = "windows" ]; then
            zip -q -j "$archive" "$binary"
            unzip -tq "$archive"
          else
            tar -czf "$archive" -C "{{.DISTDIR}}" "$binary_name"
            tar -tzf "$archive" >/dev/null
          fi

          (cd "{{.DISTDIR}}" && sha256sum "$(basename "$archive")" > "$(basename "$archive").sha256")
          rm -f "$binary"
        done
      - 'echo "완료: {{.DISTDIR}}/"'
      - ls -lh {{.DISTDIR}}/

  install:
    desc: "~/.local/bin 에 바이너리 설치 (Unix 전용)"
    platforms: [linux, darwin]
    deps: [build]
    cmds:
      - mkdir -p {{.BINDIR}}
      - install -m 755 {{.BIN}} {{.BINDIR}}/{{.BIN}}
      - 'echo "설치 완료: {{.BINDIR}}/{{.BIN}}"'

  uninstall:
    desc: 설치된 바이너리 삭제 (Unix 전용)
    platforms: [linux, darwin]
    cmds:
      - rm -f {{.BINDIR}}/{{.BIN}}
      - 'echo "삭제 완료: {{.BINDIR}}/{{.BIN}}"'

  clean:
    desc: 빌드 결과물 삭제
    cmds:
      - rm -f {{.BIN}} {{.BIN}}.exe

  clean-dist:
    desc: dist/ 디렉토리 삭제
    cmds:
      - rm -rf {{.DISTDIR}}
